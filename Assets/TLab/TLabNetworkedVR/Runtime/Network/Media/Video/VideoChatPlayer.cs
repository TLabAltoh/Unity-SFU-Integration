using UnityEngine;

namespace TLab.NetworkedVR.WebRTC.Video
{
    [AddComponentMenu("TLab/NetworkedVR/Video Chat Player (TLab)")]
    public class VideoChatPlayer : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
