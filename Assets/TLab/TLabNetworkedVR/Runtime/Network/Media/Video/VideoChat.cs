using System.Collections;
using UnityEngine;
using Unity.WebRTC;
using TLab.NetworkedVR.Network;

namespace TLab.NetworkedVR.WebRTC.Video
{
    [AddComponentMenu("TLab/NetworkedVR/Video Chat (TLab)")]
    public class VideoChat : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
