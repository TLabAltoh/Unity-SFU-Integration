using UnityEngine;

namespace TLab.SFU.WebRTC.Video
{
    [AddComponentMenu("TLab/SFU/Video Chat Player (TLab)")]
    public class VideoChatPlayer : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
